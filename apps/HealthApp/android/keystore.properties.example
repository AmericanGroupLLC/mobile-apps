# Copy this file to keystore.properties (untracked) and fill in real values
# before building a signed release. CI populates this file automatically from
# the ANDROID_KEYSTORE_* secrets — see .github/workflows/release-binaries-only.yml.
storeFile=keystore/upload.jks
storePassword=changeme
keyAlias=myhealth
keyPassword=changeme
