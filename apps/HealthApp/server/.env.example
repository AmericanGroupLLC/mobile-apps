# Copy this to .env and adjust before running `npm run dev`.
PORT=4000
JWT_SECRET=replace-me-with-a-random-32-byte-string
# DB_PATH supports any writable filesystem path. SQLite WAL mode requires a
# local (non-network) path on Windows — the temp dir works everywhere.
DB_PATH=./myhealth.db
